# Pinned for reproducible conversions (operations/LIFECYCLE.md V10: bump only
# with a re-run of the office comparison set, CHANGE_MANAGEMENT.md §4).
# Deliberately small: this app holds no Azure credential and makes no
# outbound call, so azure-identity / requests are NOT installed.
azure-functions==1.21.3
jsonschema==4.23.0
# imports of the byte-verified skill scripts staged by stage_toolchain.py
openpyxl==3.1.5          # xlsx/scripts/recalc.py
python-docx==1.1.2       # docx helpers
python-pptx==1.0.2       # pptx/scripts/thumbnail.py, add_slide.py
pypdf==5.1.0             # pdf/scripts/*
pdfplumber==0.11.4       # pdf/scripts/extract_form_structure.py
pdf2image==1.17.0        # pdf/scripts/convert_pdf_to_images.py (needs poppler)
Pillow==11.0.0           # thumbnail grids
defusedxml==0.7.1        # office/validate.py, thumbnail.py (XML parsing)
lxml==5.3.0              # python-docx/python-pptx backend
